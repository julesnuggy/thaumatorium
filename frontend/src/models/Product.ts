export type Product = {
  title: string;
  description: string;
  imageName: string;
  stock: number;
}

export type ProductResponse = {
  id: string;
  title: string;
  description: string;
  imageName: string;
  stock: number;
}
