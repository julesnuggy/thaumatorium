export type Item = {
  title: string;
  description: string;
  imageName: string;
  stock: number;
}

export type ItemResponse = {
  id: string;
  title: string;
  description: string;
  imageName: string;
  stock: number;
}
