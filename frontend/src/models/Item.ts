export type Item = {
  title: string;
  description: string;
  type: string;
  imageName: string;
  stock: number;
}

export type ItemResponse = {
  id: string;
  title: string;
  description: string;
  type: string;
  imageName: string;
  stock: number;
}
